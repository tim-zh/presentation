run `activator dist`,
launch the server,
open control link from the console,
navigate between slides by keyboard,
set a mark on a slide by mouse;

all control events are propagated to everyone else watching the same address (without the control id)